// lib/presentation/screens/validate/validate_screen.dart
import 'package:flutter/material.dart';

class DeniedScreen extends StatelessWidget {
  const DeniedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    
          return Scaffold(
            appBar: AppBar(title: const Text('Denied')),
            body: Center(

           ),
          );
        }

  }
